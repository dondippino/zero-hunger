import { DataRefreshDirective } from './data-refresh.directive';

describe('DataRefreshDirective', () => {
  it('should create an instance', () => {
    const directive = new DataRefreshDirective();
    expect(directive).toBeTruthy();
  });
});
